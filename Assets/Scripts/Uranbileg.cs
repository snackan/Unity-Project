using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Uranbileg : MonoBehaviour
{
    int uranbileg;
    int gantushig;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        if (uranbileg == gantushig)
        {
            print("uranbileg gantushig");
                }
        else {
            print("uki gantushig");
                };
    }
    }
